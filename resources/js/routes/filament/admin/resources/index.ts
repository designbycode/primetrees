import species from './species'
import treeCategories from './tree-categories'
import treeStocks from './tree-stocks'
import trees from './trees'
const resources = {
    species: Object.assign(species, species),
treeCategories: Object.assign(treeCategories, treeCategories),
treeStocks: Object.assign(treeStocks, treeStocks),
trees: Object.assign(trees, trees),
}

export default resources